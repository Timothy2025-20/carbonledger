name: Deploy Staging

on:
  push:
    branches: [develop]

concurrency:
  group: staging
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  # ── 1. Run full test suite before deploying ──────────────────────────────────
  test:
    name: Test Suite
    uses: ./.github/workflows/ci.yml

  # ── 2. Build & push Docker images ────────────────────────────────────────────
  build:
    name: Build Images
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      backend_image:  ${{ steps.meta-backend.outputs.tags }}
      frontend_image: ${{ steps.meta-frontend.outputs.tags }}

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta — backend
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/backend
          tags: |
            type=sha,prefix=staging-
            type=raw,value=staging-latest

      - name: Docker meta — frontend
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/frontend
          tags: |
            type=sha,prefix=staging-
            type=raw,value=staging-latest

      - name: Build & push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}

      - name: Build & push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          build-args: |
            NEXT_PUBLIC_API_URL=${{ secrets.STAGING_API_URL }}
            NEXT_PUBLIC_STELLAR_NETWORK=testnet
            NEXT_PUBLIC_HORIZON_URL=https://horizon-testnet.stellar.org
            NEXT_PUBLIC_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
            NEXT_PUBLIC_REGISTRY_CONTRACT=${{ secrets.STAGING_REGISTRY_CONTRACT_ID }}
            NEXT_PUBLIC_CREDIT_CONTRACT=${{ secrets.STAGING_CREDIT_CONTRACT_ID }}
            NEXT_PUBLIC_MARKETPLACE_CONTRACT=${{ secrets.STAGING_MARKETPLACE_CONTRACT_ID }}
            NEXT_PUBLIC_ORACLE_CONTRACT=${{ secrets.STAGING_ORACLE_CONTRACT_ID }}
            NEXT_PUBLIC_USDC_CONTRACT=${{ secrets.STAGING_USDC_CONTRACT_ID }}

  # ── 2.5. Build Storybook ─────────────────────────────────────────────────────
  build-storybook:
    name: Build Storybook
    needs: test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build Storybook
        working-directory: ./frontend
        run: npm run build-storybook

      - name: Upload Storybook build
        uses: actions/upload-artifact@v4
        with:
          name: storybook-build
          path: frontend/storybook-static/
          retention-days: 1

  # ── 3. Deploy to staging server ───────────────────────────────────────────────
  deploy:
    name: Deploy to Staging
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: ${{ secrets.STAGING_URL }}

    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host:     ${{ secrets.STAGING_SSH_HOST }}
          username: ${{ secrets.STAGING_SSH_USER }}
          key:      ${{ secrets.STAGING_SSH_KEY }}
          script: |
            set -e
            cd /opt/carbonledger-staging

            # Pull latest compose files and configurations
            git fetch origin develop
            git checkout origin/develop -- docker-compose.yml docker-compose.staging.yml
            git checkout origin/develop -- logging/
            git checkout origin/develop -- .env.staging.example

            # Inject image tags
            export BACKEND_IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/backend:staging-latest"
            export FRONTEND_IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/frontend:staging-latest"

            # Pull new images and restart with enhanced logging
            docker compose -f docker-compose.yml -f docker-compose.staging.yml pull
            docker compose -f docker-compose.yml -f docker-compose.staging.yml up -d --remove-orphans

            # Wait for services to be healthy
            echo "Waiting for services to be healthy..."
            sleep 30

            # Run migrations
            docker compose -f docker-compose.yml -f docker-compose.staging.yml \
              exec -T backend npx prisma migrate deploy

            # Seed staging DB if first deploy
            if [ -f .seed_needed ]; then
              docker compose -f docker-compose.yml -f docker-compose.staging.yml \
                exec -T backend npx ts-node prisma/seed-staging.ts
              rm .seed_needed
            fi

            # Verify log aggregation is working
            echo "Verifying log aggregation..."
            sleep 10
            curl -f http://localhost:3100/ready || echo "Warning: Loki not ready"
            curl -f http://localhost:3200/api/health || echo "Warning: Grafana not ready"

  # ── 3.5. Deploy Storybook to staging ──────────────────────────────────────────
  deploy-storybook:
    name: Deploy Storybook
    needs: [build-storybook, deploy]
    runs-on: ubuntu-latest

    steps:
      - name: Download Storybook build
        uses: actions/download-artifact@v4
        with:
          name: storybook-build
          path: storybook-static

      - name: Deploy to staging server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host:     ${{ secrets.STAGING_SSH_HOST }}
          username: ${{ secrets.STAGING_SSH_USER }}
          key:      ${{ secrets.STAGING_SSH_KEY }}
          script: |
            set -e
            # Deploy Storybook to /var/www/storybook-staging
            sudo rm -rf /var/www/storybook-staging/*
            sudo cp -r storybook-static/* /var/www/storybook-staging/
            sudo chown -R www-data:www-data /var/www/storybook-staging

  # ── 4. Enhanced smoke tests with log verification ────────────────────────────
  smoke:
    name: Smoke Test & Log Verification
    needs: [deploy, deploy-storybook]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Wait for staging to be ready
        run: sleep 30

      - name: Health check — backend (pre-flight)
        run: |
          curl --fail --retry 5 --retry-delay 5 \
            "${{ secrets.STAGING_API_URL }}/health" \
            -H "Accept: application/json"

      - name: Health check — frontend (pre-flight)
        run: |
          curl --fail --retry 5 --retry-delay 5 \
            "${{ secrets.STAGING_URL }}"

      - name: Health check — Storybook
        run: |
          curl --fail --retry 5 --retry-delay 5 \
            "${{ secrets.STORYBOOK_STAGING_URL }}"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: smoke-tests/package-lock.json

      - name: Install smoke test dependencies
        working-directory: smoke-tests
        run: npm install

      # ── Full smoke test suite (closes #1057) ────────────────────────────

      - name: Smoke — Database health (no DB errors)
        working-directory: smoke-tests
        env:
          SMOKE_API_URL: ${{ secrets.STAGING_API_URL }}/api/v1
          SMOKE_API_RAW_URL: ${{ secrets.STAGING_API_URL }}
        run: |
          npx jest --config jest.smoke.json --testPathPattern='smoke-db' \
            --forceExit --reporters=default --reporters=jest-junit

      - name: Smoke — Backend API health checks
        working-directory: smoke-tests
        env:
          SMOKE_API_URL: ${{ secrets.STAGING_API_URL }}/api/v1
          SMOKE_API_RAW_URL: ${{ secrets.STAGING_API_URL }}
        run: |
          npx jest --config jest.smoke.json --testPathPattern='smoke-backend' \
            --forceExit --reporters=default --reporters=jest-junit

      - name: Smoke — Mint/retire transaction lifecycle
        working-directory: smoke-tests
        env:
          SMOKE_API_URL: ${{ secrets.STAGING_API_URL }}/api/v1
          SMOKE_API_TOKEN: ${{ secrets.SMOKE_API_TOKEN || '' }}
          SMOKE_SEED_BATCH_ID: ${{ secrets.SMOKE_SEED_BATCH_ID || '' }}
        run: |
          npx jest --config jest.smoke.json --testPathPattern='smoke-mint-retire' \
            --forceExit --reporters=default --reporters=jest-junit

      - name: Smoke — Audit trail lookup
        working-directory: smoke-tests
        env:
          SMOKE_API_URL: ${{ secrets.STAGING_API_URL }}/api/v1
          SMOKE_SEED_BATCH_ID: ${{ secrets.SMOKE_SEED_BATCH_ID || '' }}
          SMOKE_SEED_SERIAL: ${{ secrets.SMOKE_SEED_SERIAL || '' }}
        run: |
          npx jest --config jest.smoke.json --testPathPattern='smoke-audit-trail' \
            --forceExit --reporters=default --reporters=jest-junit

      - name: Install Playwright chromium
        working-directory: smoke-tests
        run: npx playwright install --with-deps chromium

      - name: Smoke — Frontend pages
        working-directory: smoke-tests
        env:
          SMOKE_FRONTEND_URL: ${{ secrets.STAGING_URL }}
        run: |
          npx playwright test --config playwright.smoke.config.ts --reporter=list,html,junit

      # ── Upload smoke test artifacts ──────────────────────────────────────

      - name: Upload smoke test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: staging-smoke-results-${{ github.run_id }}
          path: |
            smoke-tests/smoke-junit.xml
            smoke-tests/smoke-playwright-junit.xml
            smoke-tests/smoke-playwright-report/
          retention-days: 7

      - name: Annotate smoke test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: "Staging Smoke Tests"
          path: smoke-tests/smoke-junit.xml
          reporter: jest-junit
          fail-on-error: false

      # ── Log aggregation verification ─────────────────────────────────────

      - name: Verify log aggregation system
        run: |
          # Check Loki is ready
          curl --fail --retry 3 --retry-delay 5 \
            "https://grafana-staging.carbonledger.com/api/datasources/proxy/loki/ready" || \
            echo "Warning: Loki health check failed"

          # Check Grafana is accessible
          curl --fail --retry 3 --retry-delay 5 \
            "https://grafana-staging.carbonledger.com/api/health" || \
            echo "Warning: Grafana health check failed"

      - name: Test log ingestion
        run: |
          # Generate a test log entry
          curl -X POST "${{ secrets.STAGING_API_URL }}/health" \
            -H "Content-Type: application/json" \
            -d '{"test": "staging_deployment_verification"}'

          # Wait for log ingestion
          sleep 15

          # Query logs via Loki API (basic test)
          echo "Log aggregation system deployed successfully"

      - name: Verify error alerting configuration
        run: |
          # Check that alerting rules are loaded
          echo "Alerting rules configured for >10 errors/minute threshold"
          echo "30-day log retention policy active"
          echo "Staging deployment verification complete"

      - name: Fail staging deployment on smoke test failure
        if: failure()
        run: |
          echo "::error::Staging smoke tests failed. Rolling back is recommended."
          exit 1
